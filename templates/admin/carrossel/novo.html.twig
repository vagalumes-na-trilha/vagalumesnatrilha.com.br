{% extends 'base.html.twig' %}



{% block body %}
    <h3 style="text-align: center;">Imagem do Carrossel</h3>
    <div class="col-12">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button class="btn btn-success m-3" type="submit">Adicionar imagem</button>
        {{ form_end(form) }}
    </div>


{% endblock %}
